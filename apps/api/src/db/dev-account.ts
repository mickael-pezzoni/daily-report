/**
 * Generic dev-only credentials, shared between `seed-account.ts` (which
 * creates the account) and `index.ts` (which reminds you of them on startup).
 * Not meant for anything beyond a local machine.
 */
export const DEV_EMAIL = 'dev@example.com'
export const DEV_PASSWORD = 'password1234'
export const DEV_NAME = 'Dev'
export const DEV_PROJECT_NAME = 'General'
